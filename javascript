<h1>자바스크립트</h1>

배열과 메서드

요소 추가, 제거 메서드 
- arr.push(...items) //맨 끝에 요소 추가
- arr.pop() //맨 끝 요소 제거
- arr.shift //맨 앞 요소 제거
- arr.unshift(...item) //맨 앞에 요소 추가

## splice
- arr.splice(index[,deleteCount, 추가요소1, ..., 추가요소n])
